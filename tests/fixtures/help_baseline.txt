Usage: dograpper [OPTIONS] COMMAND [ARGS]...

  dograpper — Context Engineering Pipeline for Deterministic LLM Ingestion.

  Transforms HTML documentation into structured, deduplicated, scored and
  versioned context for ingestion by static LLMs.

  Typical pipeline:
    dograpper download <url> -o ./docs
    dograpper pack ./docs -o ./chunks --context-header --score

  Quick pipeline:   dograpper sync <url> -o ./docs

  Each subcommand has its own help: `dograpper <command> --help`.

Options:
  --version      Show the version and exit.
  -v, --verbose  Detailed (DEBUG) logging of each operation
  -q, --quiet    Suppress output except critical errors
  --config PATH  JSON config file (sections `download` and `pack`)  [default:
                 .dograpper.json]
  -h, --help     Show this message and exit.

Commands:
  doctor    Detect, report, and install heavy dependencies (wget, chromium).
  download  Mirror a documentation site locally for offline processing.
  eval      Validate packed context empirically via golden Q&A hit-rate.
  explain   Preview what the LLM will receive for a packed chunk.
  init      Generate a .dograpper.json config for your ingestion target.
  pack      Process and group files into chunks optimized for LLM ingestion.
  serve     Serve the packed context as a local MCP server (stdio).
  sync      Incremental download + pack delta in a single command.
